#!/bin/bash

# 检查参数数量是否正确
if [ $# -ne 1 ]; then
    echo "用法：$0 <正整数>"
    exit 1
fi

n=$1

# 验证参数是否为大于0的正整数
if ! [[ "$n" =~ ^[0-9]+$ ]] || [ "$n" -lt 1 ]; then
    echo "错误：参数必须是大于0的正整数。"
    exit 1
fi

# 使用数学公式计算和
sum=$((n * (n + 1) / 2))

# 输出结果
echo "1到${n}的和为：${sum}"
